# Security: serve ONLY the app's own files. Everything else (secrets, source,
# git history, config) must never be uploaded as a public asset.
*
!dashboard.html
!app.css
!app-core.js
!app-home.js
!app-chat.js
!app-data.js
!app-config.js
!app-ops.js
